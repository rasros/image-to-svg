[project]
name = "iterative-svg-refiner"
version = "0.1.0"
description = "Iterative SVG refinement using OpenAI multimodal models"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "openai>=1.0.0",
    "cairosvg>=2.7.0",
]

[project.scripts]
iterative-svg = "main:run"
